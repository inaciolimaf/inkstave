# Inkstave real-time collaboration layer

This directory will hold the **CRDT WebSocket layer** (`pycrdt` on the server,
synced with Yjs in the browser over a JWT-authenticated WebSocket). It is empty
at spec 01 — the real-time stack is built starting in **spec 28**.
