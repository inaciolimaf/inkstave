# Assets

Screenshot **placeholders** and diagram sources for the docs. The architecture
data-flow diagrams are inline Mermaid in [../architecture.md](../architecture.md);
the `*.svg` files here are placeholders for product screenshots (replace with real
captures before a marketing release — out of scope for spec 58).

- `editor.svg` — the LaTeX editor + file tree.
- `pdf-preview.svg` — the compiled PDF preview + log/problems.
- `agent-panel.svg` — the AI agent chat + diff review.
