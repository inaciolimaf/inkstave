"""First-run bootstrap & deploy plumbing (spec 57).

Advisory-locked migrations, the idempotent first-admin setup, optional demo
seeding, and fail-fast config validation — driven by ``inkstave.cli`` and the
``/api/setup`` endpoints.
"""
