"""Inkstave backend package.

This is an empty package marker for the scaffolding spec (01). Application
modules (settings, FastAPI app, routers, models) are added by later specs.
"""

__version__ = "0.0.0"
